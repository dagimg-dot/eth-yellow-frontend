mutation Login($email: String!, $password: String!) {
  login(params: { email: $email, password: $password }) {
    accessToken
    message
    success
    errors {
      message,
      extensions {
        code
      }
    }
  }
}
